# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: vroom
spec:
  selector:
    app: vroom
  type: ClusterIP
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http

