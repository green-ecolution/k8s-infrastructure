traefik:
  additionalArguments:
    - "--providers.kubernetesingress.ingressendpoint.publishedservice=traefik/traefik-ingress-controller"
    - "--log.level=DEBUG"

  logs:
    general:
      level: DEBUG
    access:
      enabled: true
