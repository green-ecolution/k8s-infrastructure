version: 2
updates:
  - package-ecosystem: helm
    directories:
      - "/argocd"
      - "/cert-manager"
      - "/external-dns"
      # - "/keycloak"
      # - "/minio"
      # - "/postgresql"
      - "/traefik-ingress"
      - "/vaultwarden"
    schedule:
      interval: daily
    open-pull-requests-limit: 5

