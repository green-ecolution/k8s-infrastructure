apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: minio

resources:
  - namespace.yaml
  - tenant.yaml
  - ingress.yaml
  - provisioning-job.yaml

generators:
  - ./secret-generator.yaml
