external-dns:
  enabled: false
  fullnameOverride: "external-dns"
  policy: "sync"
  provider:
    name: cloudflare
  env:
    - name: CF_API_TOKEN
      valueFrom:
        secretKeyRef:
          name: cloudflare-api-key
          key: apiKey
